class VedioRequestEntity {
  String? dateTime;
  String? meetingpasscode;
  String? updatedAt;
  String? uid;

  VedioRequestEntity(
      {this.dateTime, this.meetingpasscode, this.updatedAt, this.uid});
}
